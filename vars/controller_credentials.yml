---
controller_credentials:
  - name: Windows admin
    organization: Default
    update_secrets: false
    credential_type: Machine
    inputs:
      username: "{{ windows_admin_username }}"
      password: "P@$$w0rd"
      become_method: "runas"

  - name: RHEL admin
    organization: Default
    update_secrets: false
    credential_type: Machine
    inputs:
      username: "{{ rhel_admin_username }}"
      become_method: "sudo"

  - name: Cloudflare
    organization: Default
    update_secrets: false
    credential_type: "Cloudflare ACME"
    inputs:
      cloudflare_token: 'change-me'
      acme_email: "{{ acme_email }}"

  - name: Hashicorp Vault
    organization: Default
    update_secrets: false
    credential_type: "Hashicorp Vault"
    inputs:
      vault_url: "{{ vault_url }}"
      vault_token: "{{ vault_token }}"

  - name: Hashicorp Vault Lookup
    organization: Default
    update_secrets: false
    credential_type: "HashiCorp Vault Secret Lookup"
    inputs:
      url: "{{ vault_url }}"
      token: "{{ vault_token }}"
      api_version: 'v2'

  - name: Wildcard cert files
    organization: Default
    update_secrets: false
    credential_type: "Vault Cert Files"
    inputs:
      certificate_data: 'change-me'
      certificate_fullchain: 'change-me'
      certificate_private_key: 'change-me'